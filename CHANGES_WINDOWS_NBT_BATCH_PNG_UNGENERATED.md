# Windows NBT 批量操作与 PNG 未生成区域同步

## NBT 多节点批量操作

- 统一 `NbtEditorWindow` 增加批量选择模式，覆盖世界、玩家、实体、方块实体、村庄、结构以及独立 NBT 编辑工作流。
- 支持“选择 / 取消 / 全选 / 取消全选 / 复制所选 / 导出所选 / 删除所选”。
- 批量选择使用树节点本身作为当前编辑会话中的选择目标；树刷新时重新建立可见 UI，选择模式结束时清空选择。
- “全选”只作用于当前展开可见节点，与 iOS 当前可见行批量选择语义一致。
- 批量复制支持单根和连续多根 NBT 剪贴板载荷；普通粘贴入口同时接受单标签和批量剪贴板。
- 批量导出支持连续 `.nbt`、JSON NBT；单节点额外允许 `.mcstructure`。
- 批量删除会先规范化选择路径：父节点和其子节点同时选中时只删除父节点，避免重复删除；根节点和受保护标签会阻止整次批量删除。
- 删除仍只修改当前编辑器内存状态，需要点击“应用 NBT”后才写入 Windows Cache 工作副本。

## PNG 未生成区域与导出图层

- Windows PNG 导出窗口增加独立图层开关：玩家、实体、方块实体、HardcodedSpawners、村庄、出生点、网格。
- 导出图层不再在确认后继续读取地图页实时开关，避免导出过程中与 Live View 状态耦合。
- 增加“未生成区域”三种模式：`透明 / 空气 / 纹理`。
- “透明”使用真实 BGRA Alpha=0；网格、建筑高度线和对象标记随后绘制，因此仍可覆盖到透明底图上。
- Y 视图按实际已加载 Chunk 建立生成掩码；X/Z 按选定剖面所在 SubChunk 建立生成掩码，LegacyTerrain 虚拟 SubChunk 继续通过统一读取层参与判断。
- “纹理”在 Y 视图使用全局未生成区块纹理，在 X/Z 视图使用现有子区块对角纹理；“空气”保持普通背景。
- 实时地图的“未生成区块/子区块”开关及 128 格/当前区块投影规则不受导出选项影响。

## 当前剩余同步差异

- iOS 通用 NBT 插入流程在 Compound 出现同名标签时提供“覆盖 / 保留 / 取消”；Windows 当前粘贴/导入仍对冲突标签自动生成唯一名称，后续可继续同步这一交互。
- 当前 iOS 玩家 NBT 编辑器本身也是通用 NBT 树，因此“玩家背包/装备专用槽位编辑器”不再列为 iOS→Windows 同步缺口，只属于可选的新功能增强。

# CHANGES_IPHONE_UNGENERATED_TEXTURE_CONTINUITY

本次针对“未生成区块纹理相邻区块之间仍有断点、线段未对齐”的问题继续修复。

## 修复内容

1. **按统一端点重绘 3 条斜线**
   - 每个区块保留 3 条灰色斜线：
     - 左上角 → 右下角
     - 上边中点 → 右边中点
     - 左边中点 → 下边中点
   - 每条线都直接对齐到区块的角点或边中点，和相邻区块共用同一组连接点。

2. **去掉端点留缝问题**
   - 取消上一版为了防毛边使用的内部 inset 端点。
   - 现在改为让线段从区块边界外略微延伸，再通过 `clip(to:)` 裁切到区块范围内。
   - 这样视觉上可保证线段刚好贴到边界，不会在相邻区块之间留下小缝隙。

3. **提升相邻区块连续性**
   - 相邻区块的线条会在共享边界的中点或角点处连续衔接。
   - 拼接后整体效果与用户给出的示意图一致，不再出现明显断点。

4. **顺手清理不合理实现**
   - 简化纹理函数内部计算，去掉不必要的 inset 终点方案。
   - 保留 `integral` 对齐与统一绘制路径，便于维护。

## 检查结果
- `WorldMapViewController.swift` 已通过 `swiftc -parse`。
- `Scripts/test_iphone_layout_legacy_block_link.sh` 已通过。

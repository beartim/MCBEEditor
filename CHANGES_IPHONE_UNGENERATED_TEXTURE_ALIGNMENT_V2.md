# CHANGES_IPHONE_UNGENERATED_TEXTURE_ALIGNMENT_V2

本次针对“上一版修复没有效果，未生成区块纹理在相邻区块之间仍像截图那样看起来没有对齐”的问题继续修复。

## 深入分析
上一版主要修的是**每个区块内部线段端点坐标**，但从当前效果看，真正影响视觉连续性的关键问题还有一层：

- 未生成区块纹理是在**绘制区块内容阶段**先画出来的；
- 随后又绘制了**区块网格线**；
- 网格线会覆盖在斜线连接处上方，尤其在边中点衔接的两条短斜线位置，会把连接关系“切断”；
- 所以即使端点坐标已经比上一版更合理，视觉上仍然会像“没有对齐”。

也就是说，问题不只是坐标，而是**绘制顺序不合理**。

## 本次修复
### 1. 调整未生成纹理的绘制顺序
对于 `texture` 模式：
- 在正常区块/未生成区块遍历阶段，先只记录未生成区块的矩形；
- 先绘制生成区块内容；
- 再绘制区块网格；
- **最后再绘制未生成区块斜线纹理**。

这样纹理不会再被网格线拦腰盖住，相邻区块边界处的线段会直接连续显示出来。

### 2. 同步修复两个渲染路径
本次不是只改一个界面路径，而是同时调整了：
- 地图界面主渲染路径；
- 地图图片导出渲染路径。

保证界面显示与导出结果一致。

### 3. 保留上一版的边界对齐修复
上一版对 `drawUngeneratedChunkTexture` 做的共享边界坐标对齐仍然保留，因此这次是在其基础上继续修复：
- 坐标对齐继续生效；
- 绘制顺序也修正；
- 两者叠加后，视觉连续性应明显改善。

## 结果预期
修复后：
- 相邻区块之间的短斜线不再被网格线盖断；
- 线段在边中点连接处会更像一整条连续斜线；
- 导出地图图片时也会保持相同的连续效果。

## 检查结果
- `WorldMapViewController.swift` 已通过 `swiftc -parse`。
- `Scripts/test_iphone_layout_legacy_block_link.sh` 已通过。

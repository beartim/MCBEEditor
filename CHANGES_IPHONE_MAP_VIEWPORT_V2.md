# iPhone 地图 viewport 稳定性修复（二次）

- 固定地图状态文字区域高度，避免渲染前后状态文字换行改变 `scrollView.bounds.height`，从而被误算成 Z 中心位移。
- `UIScrollView.contentInsetAdjustmentBehavior` 设为 `.never`，地图只使用自己的虚拟拖动边距，避免 iPhone 安全区再次叠加纵向 inset。
- 动态渲染区域改为根据当前屏幕实际可见的世界矩形计算，包括虚拟边距中尚未渲染的部分。
- 缩小地图时，只要当前视口需要更多区块，就立即扩大 `sideChunks`；不再依赖缩放结束瞬间的 `isTracking` 状态。
- 缩放结束/拖动结束后采用延迟到 UIScrollView 完全空闲的续载调度，避免丢失唯一一次缩放扩展请求。
- 自动移动续载改为“可见区域+预加载边界是否仍被当前渲染区域包含”的判断，不再因中心跨过一个区块就重新渲染。
- 加入 2× 回差：放大后只有当前渲染范围显著过大时才缩小范围重建高精度图，避免尺寸在临界点反复震荡。
- 渲染完成后不再直接用微小中心差异继续追逐渲染，消除 iPhone 上 Z 方向持续递减的反馈循环。
- iPhone 状态信息改为固定两行的精简统计，避免通过省略号维持高度；完整错误诊断仍可点击状态区域查看。

MCBEEditor 源码检查与修正记录 · 2026-09-10

基于本次上传的 `MCBEEditor-iOS13-v1.0.0-mod-readme-ColorsTxt-source.zip` 修改。软件版本保持 1.0.0（100），最低系统保持 iOS 13.0。

本次按源码检查了 NBT 二进制解析、旧方块格式转换、地图颜色与 X/Z 剖面、命令文件与结果输出、压缩桥接和相关数值转换。以下问题依据源码控制流程与数据类型确定；当前环境没有 Swift 编译器和 Xcode，未进行 Swift 类型检查、iOS 构建、模拟器或真机运行。新增行为测试已附带，不能将静态检查结果理解为运行测试通过。

**方块颜色与旧存档转换**

- 现代 NBT 调色板原先只按 `name` 配色，忽略 `states`，使同名的不同颜色羊毛、混凝土、木材、树叶等显示为同一色。新增共用颜色输入，识别已知的颜色、木材、石材、沙子、海晶石、海绵和花卉状态，并识别历史调色板的 `val`。原始方块 NBT 不会因显示颜色而改写。
- 平面地图和 X/Z 剖面采用同一套状态解析；缓存键包含解析后的颜色变体，避免不同状态互相复用颜色。X/Z 投影只在最终可见采样点解析颜色，避免对射线经过的每一个方块重复解析。
- 调整规则顺序，修复苔藓地毯、苍白苔藓地毯被地毯染色规则覆盖的问题；颜色词只匹配方块名开头的完整词，不再从命名空间或其他单词中误取颜色。
- 补充红色下界砖楼梯、台阶、墙，以及旧版木台阶、湿海绵、深色海晶石等变体；修复深板岩矿石颜色分支不可达的问题。
- 修正旧格式转换中的数字编号：Bedrock 的混凝土和混凝土粉末为 236/237；原代码错误使用 251/252，后两者在项目的 Bedrock 表中是侦测器和结构方块。现在 251/252 的非零 `val` 会保留，并补齐项目染色表中遗漏的编号。

典型颜色变化如下。内置颜色采用材质代表色，不等同于每张原版纹理的精确平均值。

| 方块或状态 | 原代码颜色 | 修正后颜色 |
| --- | --- | --- |
| 苔藓地毯 | `#F4F4F4` | `#5E9B3B` |
| 苍白苔藓地毯 | `#F4F4F4` | `#87947F` |
| `wool` + `color=red` | `#F4F4F4` | `#B02E26` |
| `planks` + `wood_type=spruce` | `#8B6336` | `#6B4A2B` |
| 红色下界砖楼梯/台阶/墙 | `#4A1E25` | `#672934` |
| 深板岩钻石矿石（普通地表模式） | `#3F4245` | `#535557` |
| 旧版深色橡木台阶，ID 157/158、data=5 | `#8B6336` | `#4B3422` |
| 旧版深色海晶石，ID 168、data=1 | `#5E9B8B` | `#335B51` |
| 旧版湿海绵，ID 19、data=1 | `#C9BC3B` | `#9E9B3F` |

向日葵及 `sun_flower` 别名、灵魂沙、末影箱的已有正确色值继续保留。已有覆盖全部 256 个旧数字编号及多个现代方块族的颜色测试也保留；本次未运行这些 Swift 测试。

**稳定性修正**

- NBT List、IntArray、LongArray 在分配数组前先检查声明数量是否可能被剩余字节容纳；初始预留最多 4096 项，后续随实际解析增长。避免只有几个字节的损坏文件因虚假长度立即预留巨大内存。
- VarInt 检查 32/64 位末字节的有效位，拒绝溢出及不完整数据，避免静默截断。二进制读取与十六进制显示支持起始索引不为 0 的 Data 切片；长度和偏移检查避免整数相加溢出。
- 新增共用 NBT 整数转换方法，替换实体、玩家、村庄、方块编辑、区域操作和地图等处的八套重复转换分支。NaN、无穷大和超出 Int64 的值返回失败；有效数据沿用各调用点原有的截断或四舍五入规则。玩家等级另按有限整数校验。
- 修正极大区块距离换算与经验等级计算的整数溢出边界。
- 合并 Raw Deflate 与 GZip/Zlib 的重复解压循环，按固定 64 KiB 输出窗口逐步读取。ZIP 严格核对声明长度，在输出超出声明时停止；GZip/Zlib 的预计大小仍作为估计值使用。CRC32 分段处理全部输入，避免超大输入被截断。

**命令、覆盖文件与冗余清理**

- `Command.txt` 正确处理 UTF-8 BOM、CRLF/LF/CR 换行，错误提示行号对应实际文本行，空行继续忽略。
- 命令文件预检查阶段也锁定关闭和滑动退出，检查失败时恢复操作；语法全部通过后仍按原有方式逐条执行。
- 合并单条命令与批量命令的结果输出逻辑。一次结果中的所有行保留文本、顺序和颜色，仅合并文本布局与滚动更新，减少大量区块查询结果带来的重复界面刷新。
- `Colors.txt` 支持 UTF-8 BOM，错误行忽略、最后一个有效重复条目优先的规则保持一致。先读取文字覆盖，跳过对应 PNG 的无用解码。
- PNG 使用最大 64×64 的缩略采样，按可见像素的 alpha 权重计算代表色，降低大图的解码内存需求。PNG 扫描顺序固定，避免大小写不同的重复名称产生不确定结果。
- 清理颜色表中的不可达条件、重复判断和每次查询临时构造的木材 Set。
- 已逐字比较两个内嵌 ReadMe 文本，内容与上传版一致；版本、分享设置和固定 ReadMe 重置行为保持一致。

**验证记录**

| 检查项 | 本次结果 |
| --- | --- |
| 118 个 Swift 文件的字符串、注释及括号词法结构检查 | 通过；不等同于 Swift 语法或类型检查 |
| 24 个 Shell 脚本的 `bash -n` 检查 | 通过 |
| 命令文件、纹理覆盖、chunk/XZ 的现有源码规则检查 | 通过；仅执行编译步骤之前的静态部分 |
| 旧版 zlib 写入兼容与选区导出检查、手机文字与地图稳定性源码检查 | 通过 |
| Plist/JSON 解析、两个固定 ReadMe 逐字比较、版本与项目配置比较 | 通过 |
| 差异空白检查 | 未发现空白格式错误 |
| Swift 核心行为测试 | 未运行：环境缺少 `swiftc`，新增脚本明确以 127 退出 |
| iOS XCTest、Xcode 构建和真机验证 | 未运行：环境缺少 Xcode/iOS SDK |

新增 `Scripts/test_source_audit.sh`，已接入 `Scripts/run_core_tests.sh`，覆盖状态配色、旧编号转换、CRLF/BOM、Data 切片、VarInt 边界、虚假 NBT 长度、有效 NBT 往返、浮点异常和距离溢出。

新增 `Tests/SourceAuditTests.swift`，用于 Xcode 中检查多窗口解压、ZIP 声明长度不匹配、GZip/Zlib 估计长度、透明 PNG、文字颜色覆盖优先级和异常玩家等级。

在具备 Swift 的环境中可运行 `bash Scripts/test_source_audit.sh` 或 `bash Scripts/run_core_tests.sh`。在原有 Xcode 构建流程中生成工程后，运行 `MCBEEditorTests`，并重点实机检查旧存档方块编辑、同名多状态方块地图、X/Z 导出和批量命令执行。

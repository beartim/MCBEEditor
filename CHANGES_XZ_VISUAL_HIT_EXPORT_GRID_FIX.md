# X/Z 剖面纹理、网格、对象点击与导出修复

本轮基于 `MCBEEditor-iOS13-v1.0.0-nbt-tree-xyz-cross-section-closed-range-fix-source` 继续修改。

## X/Z 未生成 SubChunk 纹理

- 删除旧的逐采样像素深色黑纹实现。
- 未生成 SubChunk 现在先使用与 Y 模式相同的浅灰底色（0.90 white）。
- 再使用与 Y 模式相同的柔和灰色（0.66 white）连续对角线纹理。
- 对角线启用抗锯齿，并在相邻缺失采样单元之间保持连续，不再出现粗黑锯齿。
- 透明导出仍保持透明，不受纹理绘制影响。

## 子区块网格

- X/Z 剖面降采样步长改为 2 的幂，常见 1/2/4/8/16 步长都能让 16 方块 SubChunk 边界落在采样单元边缘。
- 网格位置直接按世界 16 方块边界映射到渲染边缘，不再通过会产生中间偏移的整数取整方案。
- 网格线宽改为与 Y 模式相近的细线（基础 0.15），透明度降为 0.28。

## X/Z 实体/方块实体读取与点击

- 当前切面读取实体/方块实体时，垂直于切面的坐标允许 `±0.99`：
  - X 剖面：`abs(entityX - fixedX) <= 0.99`
  - Z 剖面：`abs(entityZ - fixedZ) <= 0.99`
- X/Z 单击现在先进行地图对象屏幕命中测试：
  - 实体/方块实体命中后直接选中、闪烁并打开详情。
  - 玩家和出生点继续可直接点击。
  - HardcodedSpawners 在剖面矩形内也可直接点击打开编辑。
  - 只有未命中这些对象时才进入“选择 X/Z 轴方块”。
- X/Z 导出中的实体/方块实体也使用相同 `±0.99` 切面范围。

## X/Z 导出清晰度

- 新增剖面渲染参数 `pixelsPerBlock` 与 `maximumRasterSide`。
- 屏幕实时渲染保持原有内存控制。
- X/Z PNG 导出使用 2 像素/方块目标分辨率，并把最大栅格边长提高到 4096。
- 大范围导出仍会自动降采样以限制内存，不取消“全部已加载剖面”遍历能力。

## 所有地图图片导出的独立网格选项

- `MapImageExportLayers` 新增 `grid`。
- “导出地图图片” -> “附加地图对象图层”新增“网格”选项：
  - Y 模式：区块网格。
  - X/Z 模式：子区块网格。
- 默认关闭。
- 以后导出的 PNG 是否包含网格只看这个导出选项，不再跟随地图页面当前的网格开关。
- 当前区域、框选区域、全部已加载区域以及 X/Z 剖面导出都统一遵守此选项。

## 回归保护

`Scripts/test_nbt_tree_cross_section.sh` 增加检查，覆盖：

- X/Z `±0.99` 对象切面范围；
- X/Z 对象直接点击；
- 浅灰未生成 SubChunk 纹理；
- 细线、边界对齐的 SubChunk 网格；
- 独立导出网格选项及默认关闭；
- X/Z 2 px/block、4096 边长的高分辨率导出。

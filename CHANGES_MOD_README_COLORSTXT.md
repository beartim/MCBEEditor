# Mod.zip ReadMe / Colors.txt update

- Replaced the embedded `Textures/ReadMe.txt` text verbatim with `Mod.zip/Textures/ReadMe.txt`.
- Replaced the embedded `Commands/ReadMe.txt` text verbatim with `Mod.zip/Commands/ReadMe.txt`.
- Changed the texture color override filename from `colors.txt` to the exact case-sensitive name `Colors.txt`.
- `Textures/ReadMe.txt` and `Commands/ReadMe.txt` remain fixed embedded text and are reset by the existing startup preparation flow.
- No Command.txt execution behavior or other map/command behavior was changed.

# 固定版本 1.0.0：列表查看状态、NBT 退出保护与命令同步

- 为区块、实体、框选对象、方块搜索、玩家、村庄、结构、元数据、数据库、常加载区域、生物群系、HardcodedSpawners、经验玩家、主页世界及功能入口等可进入的列表增加会话级“已查看”标签。
- 打开项目后显示黄色圆角“已查看”；点按标签弹出“清除此查看”，确认后恢复未查看状态。
- 查看状态只保存在当前控制器内存中；列表重新读取、刷新或退出编辑器后不会保存。
- NBT 标签树也按标签路径记录查看状态；批量选择模式仍优先显示勾选状态，不与“已查看”混用。
- 修复 NBT 工具与元数据搜索把祖先路径、十六进制键或根序号的模糊命中误当作相关标签的问题。普通关键词只匹配当前标签；显式路径查询仍支持 `/`、`[`、`]`。
- 区块列表支持 `(x,z)` 格式的精确坐标搜索，允许空格和 Int32 范围内的负坐标。
- 世界、玩家、村庄、结构、实体/方块实体、元数据和独立文件 NBT 编辑器在有未保存修改时拦截返回及侧滑返回，提供“取消”“不保存直接退出”“保存退出”。
- 命令写入后统一发送世界变化通知；区块、实体、玩家、村庄、结构、元数据、数据库、常加载区域、生物群系、HardcodedSpawners、地图、天气、时间和经验界面会重新读取数据。
- 打开的 NBT 编辑器在无本地修改时自动重新读取；存在未保存修改时保留本地内容并提示，避免命令结果静默覆盖编辑中的数据。
- 新增 `Scripts/test_viewed_unsaved_sync.sh`，覆盖区块坐标解析、NBT 搜索精确性、查看标签、退出保护及命令刷新挂钩。

软件版本保持 **1.0.0（100）**。

# NBT 单击与 X/Z 剖面补全修复

本次修改在上一版 NBT 树形缩进 / X-Y-Z 剖面功能基础上完成以下修复与补全：

- 修复共享 `NBTTreeCell` 中横向 `UIScrollView` 截获轻点，导致 NBT 标签无法单击展开/编辑的问题。真正的轻点会转发给原 `UITableViewDelegate.didSelectRowAt`；横向滚动、到头后再次左滑删除以及长按上下文菜单保持原行为。
- X/Z 子区块网格按实际 16 方块边界并贴合渲染采样单元边缘绘制。
- Y 模式渲染中心 Y 默认为 0；进入 X/Z 模式时默认为 63；X/Z 模式切换维度时再次重置为 63。
- X/Z 方块轴选择范围固定为当前 X/Z 坐标 ±128，并维持正坐标在上、负坐标在下。
- X/Z 选择方块后在剖面中投影到点击位置并使用黄色高亮闪烁。
- X/Z 生物群系使用当前切片方块的 3D biome 值；常加载与史莱姆模式使用当前切面所在区块的属性。
- X/Z 的未生成显示改为基于 SubChunk 是否存在，并在 UI 中显示为“显示未生成子区块”。
- X/Z 地图对象图层保留玩家、实体、方块实体、HardcodedSpawners、出生点、未生成 SubChunk 与建筑高度限制；村庄图层在 X/Z 隐藏。
- X/Z 导出改为直接生成对应剖面 PNG。当前区域导出当前剖面；“当前维度全部已加载区域”沿当前剖面的水平轴遍历所有相交已加载区块，并遍历其已加载 SubChunk Y 范围，不受 ±128 方块选择范围限制。
- X/Z 导出选项隐藏村庄层，并将未生成显示文案切换为 SubChunk 语义。
- 更新回归检查，确保 Y/X/Z 默认值、NBT 轻点、选择闪烁、剖面属性读取、SubChunk 网格、村庄隐藏和全区域剖面导出不退化。

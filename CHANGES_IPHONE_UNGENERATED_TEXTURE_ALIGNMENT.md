# CHANGES_IPHONE_UNGENERATED_TEXTURE_ALIGNMENT

本次继续修复“未生成区块纹理中，主对角线已经正常，但其余两条斜线在相邻区块之间仍未对齐”的问题。

## 原因分析
- 上一版虽然已经把线条端点延伸到区块边界外再裁切，但内部仍使用 `CGRect.integral` 做每个区块自己的独立扩展。
- `integral` 会对每个矩形分别向外取整，导致相邻区块在边界两侧得到的矩形并不完全共享同一组像素边界。
- 结果就是：主对角线由于跨角点更不明显，而另外两条接到边中点的线，在相邻区块之间看起来会略微错位。

## 修复内容
1. **改为共享边界坐标对齐**
   - 不再直接对每个区块用 `rect.integral`。
   - 改为分别对 `minX / maxX / minY / maxY` 做一致的 rounded 对齐，再用这些共同边界重新构造 `alignedRect`。
   - 这样相邻区块在共享边上的端点坐标完全一致。

2. **重新计算边中点**
   - `midX` 与 `midY` 现在基于共享边界坐标计算，而不是基于独立扩展后的 `integral rect`。
   - 保证：
     - 上边中点 ↔ 右边中点
     - 左边中点 ↔ 下边中点
     在相邻区块之间都落在同一条连续纹理线上。

3. **保留轻微外延裁切**
   - 线段仍会向边界外略微延伸，再裁切到区块内部。
   - 这样可以避免边缘留缝，同时保持连续性。

4. **顺手优化代码可读性**
   - 在函数内补充注释，明确说明这次调整的目标是让相邻区块共用同一组边界坐标。

## 检查结果
- `WorldMapViewController.swift` 已通过 `swiftc -parse`。
- `Scripts/test_iphone_layout_legacy_block_link.sh` 已通过。

# Command arrows and PNG range synchronization

- iOS 命令页右上角布局：↑ ↓ ← → 键盘 清屏。
- ↑/↓ 填入上一条/下一条本次会话命令；越过最新历史后恢复进入历史前的草稿。
- ←/→ 移动真实输入 caret，光标前后文本分开镜像，键盘隐藏时也可以调整位置。
- Windows PNG 导出新增 x1/z1/x2/z2 与四个 ±∞ 边界，保留 x+/x-/y+/y-/z+/z- 六方向。
- ±∞ 解析为当前维度已加载区块外接边界；Y± 精确裁剪矩形，X/Z 使用指定轴范围作为投影区间。
- Windows 精确范围每个 X/Z 方向最多 4096 方块，输出最长边自动压到 4096 像素以内。

# iPhone 顶部地图控制区完整文字与空间布局优化

本轮仅调整 `WorldMapViewController` 顶部地图控制区在 iPhone 竖屏下的排版，不改变地图渲染、缩放、坐标换算或存档读写逻辑。

## 修改内容

- 第一行继续固定放置三个开关，但 iPhone 不再使用缩写：
  - `自动渲染`
  - `区块网格`
  - `选择区块`
- 第二行不再把 `渲染中心坐标` 缩写成 `中心`，iPhone 与 iPad 使用同一完整文字。
- 两行均改成“按内容本身宽度布局后整体居中”，不再使用 `.equalCentering` 把控件强行分散到整行宽度，因此不会出现大块无意义的横向空白。
- 第一行三个“文字 + 开关”始终作为完整小组排列，组间使用固定间距。
- 第二行把 `渲染中心坐标 / X / Z / 渲染` 作为一个连续控件组排列。
- iPhone 的 X/Z 输入框由 46 pt 增加到 56 pt，减少数字被压缩的概率，同时利用原先空闲的横向空间。
- iPhone 开关由 0.56 倍调整为 0.59 倍，并略增开关容器宽度，使开关与完整标签的视觉比例更协调。
- iPad 布局保持原逻辑。

## 回归保护

`Scripts/test_iphone_layout_legacy_block_link.sh` 已更新，检查：

- iPhone 仍为两行布局；
- 两行均使用 intrinsic-width 内容并整体居中；
- 不再出现 `自动 / 网格 / 区块 / 中心` 的手机专用缩写；
- `渲染中心坐标` 保持完整文字；
- 之前的方块 NBT、旧数字 ID `name ↔ legacy_id` 绑定保护仍然存在。

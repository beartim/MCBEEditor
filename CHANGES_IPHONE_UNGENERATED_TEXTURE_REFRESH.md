# CHANGES_IPHONE_UNGENERATED_TEXTURE_REFRESH

本次修复与优化内容：

## 1. 重新制作未生成区块纹理
- 将“未生成区块显示 = 纹理”改为**仅绘制 3 条左上到右下的灰色斜线**。
- 线条现在会**贯穿整个区块**，不再只出现在中间一段。
- 调整绘制方式，使用 `clip(to:)` 后让线段从区块外延伸并裁切到区块内部，确保每条线都完整穿过区块。
- 线条颜色改为更清晰的纯灰色，取消半透明发虚效果。
- 线帽改为 `.square`，避免之前圆角导致视觉上“未贴边、未贯穿”的问题。

## 2. 修复纹理模式的多余底色
- 之前“纹理”模式仍会额外绘制浅灰底色，看起来像保留了不需要的背景。
- 现在“纹理”模式只绘制斜线纹理，不再叠加空气底色。
- “空气”模式仍保持原有浅灰底色逻辑。
- “透明”模式仍保持完全透明。

## 3. 优化未生成区块绘制代码结构
- 新增统一的 `drawUngeneratedChunkPlaceholder(...)`，将 `transparent / air / texture` 三种显示逻辑集中处理。
- 地图主视图与导出图片的未生成区块绘制逻辑统一，减少重复分支。
- 导出图片的 `opaque` 设置改为仅在“空气”模式下启用，避免“纹理”模式被错误当作不透明图片处理。

## 4. 继续微调 iPhone 顶部两行布局
- 让顶部前两行内容继续保持等长。
- 适当增大行内元素间距：
  - 开关行间距增大
  - 渲染中心行间距增大
  - 两行之间垂直间距增大
- 让 iPhone 上顶部区域比上一版更疏朗，不再显得过于拥挤。

## 5. 同步测试
- 更新 `Scripts/test_iphone_layout_legacy_block_link.sh`，同步校验新的 iPhone 顶部布局参数。

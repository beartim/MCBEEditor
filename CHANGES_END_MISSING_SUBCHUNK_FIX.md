# 末地高层未生成子区块修复（iOS / Windows）

基于 上一版 Windows 源码包 修改，软件版本保持 1.0.0。

## 本次定位

上传的测试存档中，末地共有 7,535 条 SubChunk 记录：2,454 条 v1、5,081 条 v8。区块使用 `LegacyVersion`（0x76，值 0x13），v8 调色板中的方块版本为 `17825808`。这是“旧区块元数据 + 调色板方块”的组合，不能仅凭 `LegacyVersion` 推断为数字 ID 方块，也不能直接套用编辑器默认的较新方块版本 `18153728`。

- iOS 原先给未生成位置返回空 layers，编辑面板据此生成带默认新版本号的空气模板；保存时会优先沿用该模板里的版本，导致新建方块没有继承存档实际版本。原有 v8 新建测试手工填入正确版本，未覆盖真实的“地图读取 → 编辑模板 → 保存”路径。
- Windows 的 fill/setblock/clone 对缺失子区块主要依据 `LegacyVersion` 选择数字 ID 格式，或固定新建 v9；没有沿用目标区块的 v1/v8。Windows 地图还会清除未生成剖面位置的选中状态，并在编辑入口拒绝未生成位置。
- iOS 命令的方块版本回退只抽取任意 32 个区块。末地存在大量只有元数据的区块，且历史 v1 调色板可以不带 version，这种抽样不能保证找到实际保存的版本。

## 修改

- iOS 的 Y 轴方块列表、X/Z 轴选择器：为缺失子区块生成与目标区块/维度兼容的空气编辑模板，同时保持“未生成”标记，直到实际保存才创建数据库记录。
- iOS 新建子区块的空气调色板优先继承目标区块中的版本；命令版本回退改用完整的维度格式推断。
- Windows 新增共用的方块格式推断，依次参考目标区块、同一维度的真实 SubChunk；用于方块编辑模板、新建、fill/setblock、clone 和缺失位置的 getblock 输出。
- Windows X/Z 剖面的未生成位置可以选中并打开方块编辑器，保存后建立对应子区块。
- 保留 v8/v9 的 255 个 storage 支持（索引 0…254）；Windows 的直接写入入口也在写入前拒绝索引 255，避免创建无法编码的第 256 层。
- 修改已生成方块仍使用原有写入路径，测试要求既有地形和区块元数据逐字节保持不变。

## 验证与范围

本环境已完成：

- 从原存档的 SST/WAL 数据重建有效记录，检查全部 7,535 条末地 SubChunk 的结构，无解析错误。
- 提取 16 条原始地形/元数据记录作为两端共用测试样本，包含 v8 区块 `(0,0)`、v1 区块 `(-121,-58)` 和只有元数据的区块 `(0,7)`；核对三个区块的 Y=160、200、255 均未生成对应子区块。
- 修改文件的字符串/注释/花括号检查、Shell 语法检查、项目 XML 和测试输入路径检查通过。这些检查不等同于编译或类型检查。
- 核对原有资源和 iOS 项目配置未变；测试存档原件未修改。

新增回归用例已接入现有测试入口，但本环境没有 Swift、.NET SDK、Xcode 或 Windows GUI，**未执行两端的编译、核心行为测试、应用运行或 Minecraft 重载验证**。

在具备相应工具链的环境中运行：

```bash
bash Scripts/test_end_missing_subchunks.sh
# 或运行现有完整 iOS 核心测试入口
bash Scripts/run_core_tests.sh
```

```powershell
dotnet run --project Windows/MCBEEditor.Core.SelfTest/MCBEEditor.Core.SelfTest.csproj -c Release
# Windows/build.cmd 会按原流程运行核心测试并构建应用
```

新用例覆盖真实地图编辑模板、v1/v8 与仅元数据区块的高层创建、坐标读回、原记录不变、iOS 先创建 layer1、Windows 1/3/255 storage 和 clone。数据库接口使用内存替身，编码/解码及编辑逻辑使用产品源码；即使这些测试在工具链环境中通过，仍需在原测试存档副本中检查保存并重新打开后的效果。

# X/Z 网格、矿物视图、导出与 `chunk` 命令修正

## 1. X/Z 子区块网格对齐

- 子区块网格路径继续使用精确的 16 方块世界坐标边界。
- 调整 Core Graphics 栅格化方式，使网格线以路径为中心向两侧绘制，而不是让线条的一侧边缘吸附到方块边界。
- 未生成子区块纹理仍与同一套世界坐标到像素坐标变换共用边界，缩放和移动后不会产生独立偏移。

## 2. X/Z 导出未生成子区块默认显示为空气

- X/Z 导出选项中的“未生成区块显示”默认值改为“空气”；Y 模式保持原有默认值。
- X/Z 导出选择“空气”时，会用空气背景覆盖当前剖面上不存在的 SubChunk，避免 128 方块投影把后方已生成地形透到未生成区域中。
- “透明”和“纹理”选项仍保留原有行为。

## 3. X/Z 矿物视图读取与默认选择

- 矿物视图投影范围改为包含两个端点：当前 X/Z 到当前 X/Z - 128。
- 点击矿物视图方块时，读取器同样只读取当前 X/Z 到当前 X/Z - 128。
- 默认选择按 X/Z 从大到小寻找目标矿物，因此会直接选中该范围内 X/Z 最大的矿物方块，而不是石头等普通非空气方块。
- 非矿物模式保留原有方块选择行为。

## 4. 新增 `chunk` 命令

支持：

```text
chunk query
chunk query <dimension>
chunk query <dimension> <chunkX> <chunkZ>
chunk empty [dimension] <chunkX> <chunkZ>
chunk regenerate [dimension] <chunkX> <chunkZ>
```

行为：

- `chunk query`：按 `overworld -> nether -> the_end` 顺序列出所有已加载区块；每个区块单独一行并使用命令界面的蓝色输出样式，同时显示区块信息与生成状态。
- `chunk query <dimension>`：只列出指定维度。
- `chunk query <dimension> <x> <z>`：查询指定区块；不存在记录时显示“未生成”。
- `chunk empty`：复用区块栏“清空”逻辑，删除原区块/实体记录并写入已生成空气区块元数据。
- `chunk regenerate`：复用区块栏“重新生成”逻辑，删除区块及关联实体记录，使 Minecraft 下次加载时按种子重新生成。
- 为兼容“empty/regenerate 后只跟 X+Z”的输入方式，省略维度时默认 `overworld`；同时允许显式指定维度。

## 5. 回归检查

- 增加 `chunk` 命令解析测试与实际 LevelDB 执行测试。
- 覆盖三维度查询顺序、蓝色输出类型、空气区块生成状态、清空和重新生成。
- 更新 X/Z 剖面静态回归检查，覆盖网格中心对齐、矿物优先选择、X/Z 导出空气默认值和 Xray -128 端点。

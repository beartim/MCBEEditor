# CHANGES_IPHONE_TEXTURE_TOP_ROWS_TUNE

本次继续优化：

1. **未生成区块纹理简化**
   - 将原先较复杂且有锯齿感的纹理，改为每个区块仅绘制 **3 条从左上到右下的灰色斜线**。
   - 去除其余点阵、反向线条、半透明铺底等装饰内容。
   - 启用抗锯齿与圆角线帽，改善观感。

2. **iPhone 顶部前两行布局优化**
   - 让顶部两行内容的视觉长度保持一致。
   - 第一行（开关行）与第二行（渲染中心行）在 iPhone 上共享相同宽度。
   - 两行内部改为 `equalSpacing`，让内容比上一版更松一些，但不过度分散。
   - 两行之间的垂直间距从 6 调整为 8。

3. **回归测试同步**
   - 更新 `Scripts/test_iphone_layout_legacy_block_link.sh`，同步检查新的两行布局参数。

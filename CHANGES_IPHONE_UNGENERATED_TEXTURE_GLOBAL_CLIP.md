# CHANGES_IPHONE_UNGENERATED_TEXTURE_GLOBAL_CLIP

## 本次修复

针对“未生成区块纹理除主对角线外其余线段在相邻区块之间仍未对齐、隐藏区块网格时线段不能连续、对角线端点留缝”的问题，改为 **整批未生成区块统一裁剪后绘制全局斜线场**，不再逐区块单独画三条线。

### 具体调整
- 将纹理绘制方式从“每个区块单独绘制局部线段”改为：
  - 先收集所有未生成区块矩形；
  - 使用这些矩形构建统一 clip；
  - 在 clip 内按固定相位绘制整批平行对角线；
  - 线条使用 `.square` line cap，避免端点留缝。
- 纹理底色仍保持浅灰底。
- 当前地图查看路径与导出图片路径统一使用同一套批量纹理逻辑。
- 保留“显示区块网格”开关；当不显示区块网格时，纹理线段会直接跨区块连续显示。

## 预期效果
- 相邻未生成区块之间的三组斜线视觉连续。
- 不再出现每个区块单独起止造成的半线段错位。
- 导出图片与界面预览保持一致。

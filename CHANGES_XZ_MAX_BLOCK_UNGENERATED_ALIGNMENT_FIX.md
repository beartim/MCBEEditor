# X/Z 最大非空气方块与未生成 SubChunk 对齐修复

本轮基于 `xz-projection-ungenerated-grid-match` 继续调整 X/Z 剖面：

- X/Z 128 方块投影现在显式按坐标从大到小扫描；普通模式取 X/Z 坐标最大的第一个非空气方块，矿物模式同样优先坐标最大的目标矿物。
- “选择 X/Z 轴方块”列表仍保持坐标从大到小，并默认定位到列表中第一个非空气方块；仅当整条选择范围均为空气/未生成时才回退到点击坐标。
- 撤销上一版“父区块已生成 => 缺失 SubChunk 视为空气”的错误兜底。X/Z 中只要对应 SubChunk 记录不存在，就按未生成处理，包括天空中实际存在的大量未生成 SubChunk。
- X/Z 未生成纹理不再按采样像素拼接。现在先按当前切面真实的 16×16 SubChunk 单元判断是否存在，再用与子区块网格完全相同的世界坐标 -> 栅格坐标换算生成裁剪矩形。
- 斜纹相位锚定世界坐标，平移或缩放后不会相对 SubChunk 网格漂移。
- 子区块网格最后绘制在斜纹之上，确保 16 方块边界在未生成纹理区域仍清晰可见。
- X/Z 渲染器不再接收 Y 模式的 `generatedChunkPositions` 作为 SubChunk 生成状态依据，避免未来再次把缺失天空 SubChunk 当成普通空气。

# Blocktopograph iOS 13 rewrite

## v1.1.17 SubChunk v8 写入与实体格式导入导出

- 修复 `LegacyVersion/Data2D + SubChunk v8` 世界在缺失高层 SubChunk 中修改方块后被游戏忽略或重新生成的问题。
- 缺失 SubChunk 优先沿用目标区块已有兄弟 SubChunk 的持久化版本；例如 Y=80 缺失时可创建兼容的 v8 SubChunk，而不混写 v7/v9。
- 实体导出只包含当前选中实体的全部标签，并支持实体 JSON、Little Endian、Little Endian VarInt 和 Big Endian NBT。
- 实体 JSON 采用 `blocktopograph-nbt-json` 的 `documents` 标签数组格式；导入同时支持单根/连续 NBT 与实体 JSON。
- 命令终端初始保持空白；主页 NBT 工具图标改为更圆润的圆角卡片样式。

## v1.1.16 构建回归修复与分维度默认中心

- 修复便携核心测试仍匹配 v1.1.13 旧写入表达式，导致完整的未加载区块生成逻辑被误报为不完整、GitHub Actions 在 Xcode 编译前退出的问题。
- 地图初次打开默认切换到本地玩家所在维度，并以玩家实际坐标为中心。
- 切换到玩家所在维度时默认回到玩家坐标；切换到另外两个维度时默认以方块坐标 `(0,0)` 为中心。
- 三个维度仍保留用户设置的图层、对象开关与缩放比例，但不再把其他维度沿用玩家维度的视口中心。
- 当前版本：**1.1.17 (127)**。

## v1.1.15 旧版 SubChunk 自动升级与完整命令 NBT

- 方块 NBT、`fill`、`clone` 在旧版数字 ID SubChunk 中写入无数字 ID 方块、非空 states 或非空气层 1 时，自动把整个区块迁移为 `Version/Data3D/v9` 后再写入。
- 可继续用旧版数字 ID 表示且 states 为空的修改仍保留旧格式。
- 命令 NBT 参数支持全部标量、ByteArray/IntArray/LongArray、List、Compound 和任意层级嵌套，空中括号与空大括号均可使用。
- `give` 改为四个参数，第四参数可为 `NULL` 或完整物品 NBT 标签列表。
- 当前版本：**1.1.15 (125)**。

## v1.1.14 命令结果、区块持久化、批量 kill 与实体模板修复

- 所有成功执行的命令结果使用绿色文字显示，错误结果继续使用红色。
- 未加载区块/SubChunk 写入时会匹配当前维度的 Version/LegacyVersion、Data3D/Data2D 和方块调色板格式；元数据、生成完成状态与目标 SubChunk 写入后逐项读回验证。
- 修复 `kill @e 1` 对同一连续 Entity 记录逐个删除导致索引失效、部分实体已删除后再报数据格式错误的问题；现在统一原子批量删除。
- `give` 仅把包含空字符串 `Name` 的 Compound 视为空槽；对非玩家实体写入主手物品时增加 `WasPickedUp=1`。
- `summon` 最后一个参数支持 `default`，表示不覆盖实体通用 NBT。
- 调整实体通用 NBT：`IsAutonomous=0`、`ShowBottom=0`，新增 `IsEating=0`，移除 LinksTag、FireImmune、HasCollision、HasGravity、HasOwner 和 Age。
- 当前版本：**1.1.14 (124)**。

## v1.1.13 实体通用 NBT、连续实体导入导出、summon 与空气区块补写

- 新建与复制实体会补齐基岩版实体通用 NBT 标签，同时保留模板或导入文件中的实体专用标签。
- 新建实体支持选择连续多根 `.nbt`：先指定维度、坐标和起始 UniqueID，再逐根预览和编辑后批量导入。
- 实体长按菜单新增“导出连续多根 NBT”，按源 LevelDB 记录导出全部连续根标签。
- 新增严格格式的 `summon` 命令，可用实体通用模板创建实体并增补/覆盖指定 NBT 标签。
- 方块 NBT、`clone`、`fill` 涉及未加载区块时，会先写入空气区块元数据和生成完成状态，再创建或修改 SubChunk。
- 地图点选无有效 Y 的列时默认选择 Y=0；命令输入光标移到终端上方；命令完成后对象列表在主线程安全重新扫描。
- `give` 对玩家优先写入第一个空物品栏槽位，物品栏满时替换最后一格。
- 当前版本：**1.1.13 (123)**。

## v1.1.12 Xcode 版本检测 Broken pipe 修复

- 修复 `Scripts/bootstrap.sh` 使用 `xcodebuild -version | awk ... exit` 时，`awk` 提前关闭管道导致 Xcode 15.4 抛出 `NSFileHandleOperationException: Broken pipe` 并以 134 退出的问题。
- Xcode 版本检测改为先完整捕获 `xcodebuild -version` 输出，再从内存文本解析版本号，不再提前关闭 `xcodebuild` 的标准输出。
- GitHub Actions 的 “Select Xcode 15.4” 步骤同步使用同一安全检测方式，避免相同问题在工程生成前发生。
- 当前版本：**1.1.12 (122)**。

## v1.1.11 本地玩家地图中心、目标选择器与实体命令

- 地图栏目默认切换到本地玩家所在维度，并以玩家实际方块坐标为视口中心；无法读取时回退到世界出生点。
- 命令输入行并入大终端面板，连续空格和末尾空格保持等宽显示，持续显示闪烁块状光标。
- 修复 `clone` 在目标 Y 与源 Y 不同或同维度重叠时源数据被目标写入污染的问题；复制前冻结全部源 SubChunk。
- 新增 `@s`、`@a`、`@e`、UniqueID 和实体 identifier 目标选择器。
- `clear` 扩展到玩家和实体物品并保留村民交易；`clearspawnpoint` 改为必须指定目标。
- 新增 `give`、`kill`、`kick`，所有命令严格限制参数数量。
- 当前版本：**1.1.11 (121)**。

## v1.1.10 命令终端稳定性、跨维度 clone/fill 与重叠复制修复

- 修复命令执行完成后后台线程发送世界变化通知导致的 UIKit 闪退；数据库写入完成后统一回到主线程刷新界面。
- `clone` 改为 `clone 源维度 x1 y1 z1 x2 y2 z2 目标维度 x3 y3 z3`，支持 `overworld`、`nether`、`the_end` 之间复制。
- `fill` 改为 `fill 目标维度 x1 y1 z1 x2 y2 z2 层0名称 层0states 层1名称 层1states`。
- 修复重叠 clone 连锁复制：使用重叠安全遍历，始终复制原始源区域内容。
- 命令栏目移除维度选择器，增加实时可见的等宽输入行和持续闪烁光标。
- 当前版本：**1.1.10 (120)**。

## v1.1.9 世界命令行

- 在 NBT 与信息之间新增“命令”栏目，提供主世界、下界、末地选择器和等宽字体命令行窗口。
- 支持 `help`、`clear`、`clearspawnpoint`、`clone`、`fill`；命令不使用斜杠，参数数量、整数坐标、UniqueID、引号和方块 states 均严格校验。
- `clear` 可按本地玩家或本地／在线玩家 UniqueID 清除物品容器；`clearspawnpoint` 删除玩家出生点标签。
- `clone` 复制层 0、层 1 和方块实体，未加载的源或目标区块直接跳过；源目标重叠时按坐标顺序直接覆盖，不使用源区域快照。
- `fill` 同时接收层 0／层 1 方块名称和 states；states 支持 `NULL` 或 `'类型'"键"="值"` 逗号列表。层 1 为空气且原位置没有层 1 时不会创建第二 storage。
- 当前版本：**1.1.9 (119)**。

## v1.1.8 方块 NBT 核心测试编译修复

- 修复 `BedrockSubChunkEditor.swift` 在 portable core tests 中调用不存在的 `BedrockBlockRecord.stateForEditing` 导致的编译失败。
- 当前图层状态改由 `BedrockBlockNBTStore` 直接从 `block.layers` 解析；缺失图层仍按已有调色板版本生成空气状态。
- 实际 App 的现代方块与旧版数字方块 NBT 保存行为保持不变。
- 当前版本：**1.1.8 (118)**。

## v1.1.7 实体存储兼容、旧版方块 NBT 与精确渲染中心

- 新建实体不再固定写入 `actorprefix/digp`：程序会根据当前世界已有记录、`ActorDigestVersion` 和复制来源，自动选择旧版区块 `Entity(0x32)` 或现代 Actor 存储；旧世界中由旧版程序误写的孤立 `digp/actorprefix` 不会被当成世界已升级的依据。
- 复制旧版区块实体时强制继续写入目标区块的 `Entity(0x32)` 连续 NBT；同时保留或按字符串 ID 更新旧版数字 `id` 标签，避免把 Short/Int 实体 ID 错改为字符串；现代世界仍使用 `actorprefix/digp`。
- 方块详情的 NBT 编辑器支持旧版数字方块：以 `legacy_id`、`legacy_data` 和对应 `name` 显示，可修改数字 ID 0–255 与数据值 0–15并写回旧版 SubChunk。
- 地图以方块坐标渲染时，视口中心精确使用输入方块的中心点 `X+0.5/Z+0.5`，不再换算并吸附到所属区块中心。
- 当前版本：**1.1.7 (117)**。

## v1.0.0 NBT工具

- 主页世界列表下方新增“NBT工具”，统一入口为“NBT/mcstructure读取修改和转换”，无需导入世界即可读取、修改与导出 `.nbt`、`.mcstructure` 文件。
- 自动识别 Big Endian、Little Endian、Little Endian VarInt、GZip/Zlib，以及多个根标签连续存储的 NBT；连续根标签序号从 0 开始。
- 根标签列表支持新建、进入修改、重命名和删除；独立编辑器支持修改根名称、全部标量与数组、增加/删除/重命名标签、复制粘贴完整标签，以及复制标量文本内容。返回时会提示保存修改。
- 格式转换可输出 Big Endian NBT、Little Endian NBT、Little Endian VarInt NBT，并可把 Java 结构 NBT 转换为 Bedrock `.mcstructure`。
- `.nbt` 与 `.mcstructure` 已注册为可从“文件”App直接打开的文档类型。
- 固定版本：**1.0.0 (100)**。

## v0.11.7 Java 结构 NBT 转换

- 结构导入不再只转换 NBT 字节序：识别 Java 结构的 `size/palette/blocks`，转换为 Bedrock `format_version/structure/block_indices/palette` mcstructure 架构后再写入世界。
- 支持 Big Endian、Little Endian、Little Endian VarInt，以及 GZip/Zlib 包装；Big Endian Short 标签也按正确字节序解析。
- 两个 `block_indices` 图层都会补齐为结构完整体积，避免 Minecraft 因第二图层长度不足而拒绝读取。
- 对照 `ship.nbt` 与 `ship.mcstructure` 示例验证，转换结果逐字节一致；未映射状态采用安全降级，实体、水层和高级方块实体数据按参考方案忽略。
- 现有 Bedrock mcstructure 会保留内容，仅规范根名称、Little Endian 编码和索引图层长度。
- 当前版本：**1.1.7 (117)**。

## v0.11.7 地图矩形区域编辑

- 地图框选完成后保留蓝色矩形，四条边使用加大的透明触控区域，便于抓取拖动；左上角持续显示 `X0/Z0/X1/Z1`，可输入坐标精确修改当前已渲染地图内的选区。
- 进入框选模式后右上角框选按钮持续闪烁；建立选区前可用两指移动或缩放地图，建立选区后空白区域触摸交还地图原生滚动/缩放，并禁止从地图重新拉出第二个选框。
- 框选完成后弹出区域操作菜单，可查看区域内实体与方块实体、复制到另一等大区域、按层搜索替换方块、执行选区内批量层 0/层 1 替换、修改生物群系、进入选区所覆盖区块的 HardcodedSpawners 编辑页。
- 区域复制支持任意方块边界，目标 `X1/Z1` 按源区域尺寸自动计算；复制层 0/层 1 方块状态、生物群系和方块实体，完整区块且不重叠时使用原始区块记录快速复制。
- “清空区域”和“重新生成区域”只按完整区块执行；选区未与 16×16 区块边界对齐时，确认页会显示向四周扩展后的实际坐标和区块数量。
- 负坐标沿用数学向下取整规则，例如 X=-1 属于区块 -1；区域扩展、复制和局部修改均使用同一坐标规则。
- 原地图导航栏中的区块列表已迁移到独立“区块”栏目；顶层工作区现在为“地图、实体、区块、NBT、命令、信息”。
- 实体栏目移除 N×N 扫描，默认扫描所选维度的全部实体与方块实体；“全部”位于主世界左侧，可跨维度显示，并支持 X/Z 矩形、可选 Y 范围，以及从指定点按 XZ/XYZ 半径筛选。
- 当前版本：**1.1.7 (117)**。

## v0.11.7 生物群系地图与 HardcodedSpawners 图层

- 地图图层新增“生物群系”，与地表、高度、矿物并列；Data3D 会按每列地表 Y 选择垂直生物群系层，Data2D/Data2DLegacy 直接使用 16×16 平面数据。
- 生物群系使用稳定颜色渲染；未知或自定义数值 ID 会生成确定性颜色，不会被隐藏。
- 地图“对象图层”新增 HardcodedSpawners 开关，开启后以粉色虚线矩形显示下界要塞、沼泽小屋、海底神殿、前哨站及未知刷怪范围；默认关闭以避免低倍率超大区域的额外数据库读取。
- 生物群系编辑器新增数字 ID—中文名称—identifier 对照表，可按 ID、中文名称、英文 identifier 或坐标搜索；修改单点或整层时直接从可搜索列表选择，也允许输入未知 UInt32 ID。
- 生物群系数字表已按 `生物群系ID.xlsx` 核对，共收录 89 项，并补充 `193 pale_garden`、`194 sulfur_caves`、`195 dappled_forest`；无法识别的 ID 继续以“未知/自定义”显示并可正常保存。
- “选择世界”主界面在世界列表下方新增“基岩版数据值”，可直接打开实体 ID、生物群系 ID、状态效果 ID 和魔咒 ID，无需先导入或选择世界；“信息”页中的同类入口继续保留。
- 实体表补充 `154 / 0x9A minecraft:cushion`，并修正旧版/新版僵尸村民、末影之眼、末影龙火球和不祥之物生成器名称；状态效果扩展到 ID 37，魔咒扩展到 ID 41。
- “清空区块”仍采用安卓版 `createEmpty` 风格；区块列表中的 Data3D/Data2D 与 HardcodedSpawners 编辑能力保持不变。
- NBT 栏目新增“村庄 NBT”，兼容旧版 `mVillages` 与新版 `VILLAGE_*` 村庄记录，可搜索、查看、修改并写回村庄信息、兴趣点、居民和玩家声望等参数。
- NBT 栏目中的世界、玩家、村庄与结构数据统一为完整树编辑：支持修改标量/数组、增加子标签、重命名、删除节点和保存。
- 地图开启区块选择并选中区块后，可长按该区块直接调出与区块列表一致的操作菜单。
- 当前版本：**1.1.7 (117)**。

## v0.11.5 区块清空崩溃与重新生成修复

- 修复“清空区块”后 Minecraft 加载该区块闪退：清空层 0/层 1 的同时删除方块实体、旧实体、现代 `digp/actorprefix` Actor、计划刻、随机刻、刷怪、校验与元数据哈希等失效依赖记录。
- “重新生成区块”改为与安卓版 `removeFullChunk` 相同的**原始键前缀删除**，不再只删除当前程序认识的标签。
- 同时兼容旧式主世界 8 字节区块前缀和现代 12 字节维度前缀；会删除 `ConversionData`、`GenerationSeed`、混合数据、`ActorDigestVersion`、`LegacyVersion` 及未来未知标签。
- 重新生成时同步删除 `digp` 摘要和摘要引用的 `actorprefix` 记录，避免留下实体孤儿。
- 当前版本：**1.1.7 (117)**。

## v0.11.4 区块清空与按种子重新生成

- 移除“批量层0层1替换”页面底部的“删除所有层0内容”“删除所有层1内容”按钮。
- 区块列表中的原“删除区块”改为“清空区块”：层 0 全部填充空气；层 1 优先移除 storage，无法移除时填充空气；区块本身仍保持已加载状态。
- 区块操作菜单新增第五项“重新生成区块”：删除该坐标全部标准区块记录和 `digp` 索引，使 Minecraft 下次加载时按照世界当前种子和版本重新生成。
- v0.11.5 起，清空区块会同时删除与空气区块不再兼容的方块实体、实体与 tick 记录，避免游戏加载崩溃；重新生成会完整删除原始区块前缀记录。
- 当前版本：**1.1.7 (117)**。

## v0.11.3 无自动备份与批量层替换

- 删除全部自动备份机制：不再创建整世界快照、`level.dat_old`、玩家/结构/实体/方块/区块备份目录。
- “信息”页保留世界信息、数据库浏览器和手动导出 `.mcworld`；不再提供快照创建、恢复和快照导出。
- 世界、玩家、村庄、结构、实体、方块 NBT，以及区块复制、删除和搜索替换均直接写入；使用前默认用户已经自行备份。
- 区块方块搜索替换页底部新增“批量层0层1替换”，在独立页面中覆盖当前区块所有现有 SubChunk 的指定层。
- 批量目标层默认为层 1；“选择双层皆为空气的方块”默认关闭。
- 目标层不存在时按需创建；层 0 / 层 1 使用与其他方块功能一致的 NBT 编辑器填写 `name` 和 `states`。
- 当前版本：**1.1.7 (117)**。

## v0.11.2 缩放驱动区块扩展与联合分层搜索替换

- 地图的 3×3、5×5、7×7、9×9 选择改为**最小预载范围**，不再是最大渲染范围。
- 双指缩小时根据当前视口、缩放比例和每方块像素数自动计算所需奇数 N×N 区域，并额外预载两圈区块；当前栅格渲染器在单次区域内采用 63×63 安全上限，继续拖动可无限续载世界坐标。
- 缩放结束、屏幕旋转或视口中心移动时，如果当前区域不足以覆盖可见范围，会围绕当前视口中心自动重新渲染，并保留绝对方块中心与缩放比例。
- 方块搜索替换页面右上角新增“查找范围”三选框：层 0、层 1、层 0 和层 1。
- 只填写一个搜索列时，该列作为通用条件，可由用户选择在哪一层或两层中查找；选择两层时任意一层匹配即可选中该坐标。
- 层 0、层 1 搜索列都填写时，查找范围自动锁定为“层 0 和层 1”，同一坐标必须分别满足两列条件。
- 搜索命中后以方块坐标为单位应用替换：层 0 始终按替换栏写入；新增“是否改变层 1”开关，默认关闭并保留原层 1。
- 开启“是否改变层 1”后，层 1 替换栏可编辑；若留空，则把匹配坐标的原层 1 删除为空气，整层完全为空时移除第二 storage。
- 所有启用的替换继续默认清空原 `states` 后写入新标签，name 留空仍保留原 name。
- 当前版本：**1.1.7 (117)**。

## v0.11.1 地图布局与分层 NBT 搜索替换

- 修复地图未选择方块时顶部控制区被纵向拉伸，以及选择方块后地图起始位置留空的问题；四行控制区使用固定内容高度。
- “渲染”改为蓝色圆角矩形按钮；自动渲染、网格和区块选择三个开关移动到渲染按钮右侧。
- 方块 NBT 的 X、Y、Z 输入框改为同一行，“跳转并查看”改为蓝色圆角按钮。
- 区块方块搜索替换改为 NBT 编辑器样式，搜索和替换均以层 0、层 1 两列并排显示。
- 每层包含 `name` 与空 `states` Compound，可通过“增加 NBT 标签”添加带类型的状态条件或替换状态。
- 不再单独选择搜索图层：只填写层 0 时仅扫描层 0，只填写层 1 时仅扫描层 1，两列都填写时同时扫描两层。
- 替换默认先清空目标方块原 `states` 再写入对应层的新标签，已移除“清空原 states”开关。
- 当前版本：**0.11.1 (16)**。

## v0.11.0 区块管理

地图新增区块列表和“点击选择区块”开关。选中的区块以橙色边框持续闪烁，并可从列表定位到地图。区块列表扫描 LevelDB 中所有标准区块键及 `digp` 实体索引，支持按维度和坐标搜索。

区块管理操作包括：

- 复制地形、SubChunk、生物群系、方块实体和安全的区块状态到目标区块；
- 清空区块层 0/层 1 内容但保留区块加载状态；
- 重新生成区块会删除全部标准记录和 `digp` 索引，使 Minecraft 按种子重新生成；
- 在层 0、层 1 或两层中按方块 `name` 与部分 `states` 参数搜索替换；
- `name`、状态键和状态值均支持不区分大小写的部分匹配；
- 当前界面按层分别填写替换 NBT；v0.11.1 起默认清空原 states 后写入带类型的新标签；
- 复制、删除和搜索替换直接使用 LevelDB `WriteBatch` 提交，不创建持久化备份；
- 多键变更通过 LevelDB `WriteBatch` 原子提交，完成后自动清除地图缓存。

为避免实体 UniqueID、tick 坐标和调度状态冲突，区块复制默认不复制实体、pending ticks、random ticks 和硬编码刷怪记录。v0.11.5 起，重新生成会同时删除 `digp` 索引及其引用的 `actorprefix` 记录。

## v0.11.0：地图布局、双层方块 NBT 与标记清理

- 修复未选中方块时，右侧面板最小高度反向拉伸顶部控制区造成的大面积空白；地图控制区始终紧贴导航栏下方。
- 方块 NBT 固定提供“层 0 / 层 1”两个编辑页；缺失的层 1 以空气层显示，保存后自动创建第二 storage。
- 单 storage 的 SubChunk v1 在创建层 1 时安全升级为支持多 storage 的 v8。
- 移除自定义地图标记按钮、长按创建、标记列表和本地标记存储；出生点定位按钮与出生点显示保留。
- v0.11.0 区块管理基线版本：**0.11.0 (15)**。

## v0.10.0：统一 NBT 结构编辑与方块状态写回

- Bundle ID 固定为 **`com.wzn.blocktopograph`**；测试 Target 为 `com.wzn.blocktopograph.tests`。
- 世界、玩家、村庄、结构、实体和方块实体 NBT 统一支持增加、删除、重命名及标量/数组修改。
- Compound/List 可增加子节点；所有非根节点可删除；Compound 标签可重命名。
- 地图右侧方块详情改为完整 NBT 树，展示 `name`、`states`、`version` 等全部调色板属性。
- 方块 NBT 修改会为目标方块复用或新增调色板项，仅重定向所选方块格，并重新编码 v1/v8/v9 SubChunk。
- 旧版数字 ID SubChunk 继续只读，遇到不支持格式时拒绝写回而不是冒险损坏世界。

这是对 Android 版 Blocktopograph 的原生 iOS 重写工程。

- 最低系统：**iOS / iPadOS 13.0**
- 构建工具：**Xcode 15.x，推荐 Xcode 15.4**
- 当前版本：**1.1.7 (117)**
- 架构：arm64
- 界面：UIKit
- 数据库：Mojang 兼容 LevelDB + Objective-C++ 桥


## 0.9.0：实体与方块实体 NBT 编辑

- 实体栏目、地图对象详情和地图框选结果均新增“编辑 NBT”；
- 支持 Byte、Short、Int、Long、Float、Double 和 String 标量修改；
- 现代实体写回会同步更新 `actorprefix` 与 `digp` 区块摘要；
- 旧版区块实体会重建 `Entity (0x32)` 连续 NBT 记录；
- 方块实体会重建 `BlockEntity (0x31)` 连续 NBT 记录；
- 修改坐标跨区块或维度时，记录会自动从原区块迁移到目标区块；
- `UniqueID` 可直接修改；保存时会同步迁移 `actorprefix` 键与 `digp` 区块摘要，仍禁止删除或重命名该标签；
- 多键写入失败时会尝试按相反顺序恢复原值；
- 编辑完成后地图和实体列表自动重新扫描。

## 0.8.0：结构 NBT 与导航重排

- 顶层工作区调整为“地图、实体、NBT、信息”四栏，实体固定在第二栏；
- 原“世界 NBT”顶层栏目重命名为“NBT”；
- NBT 首页将“世界 NBT、玩家 NBT、结构 NBT”作为三个同级入口；
- 世界 NBT 继续编辑 `level.dat`，玩家 NBT 继续编辑玩家 LevelDB 数据；
- 结构 NBT 扫描 LevelDB 中以 `structuretemplate` 开头的已保存结构；
- 结构列表显示名称、尺寸、世界原点、格式版本和数据大小；
- 结构 NBT 支持名称/键/尺寸/原点搜索、完整只读 NBT 浏览、路径和值复制；
- 结构可原样导出为 `.mcstructure`，无法解析的记录仍保留原始值供诊断；
- 原独立“数据库”栏目移入“信息 → 数据库浏览器”。

## 0.7.0：玩家 NBT、地图框选与对象选中

- `minecraft:vine` 在地表地图中固定使用绿色；
- NBT 功能包含世界 NBT 与玩家 NBT；
- 玩家 NBT 支持 `~local_player`、`LocalPlayer`、`player_…`、`player_server_…`；
- 玩家 NBT 支持全树搜索、标量修改、保存和原始二进制导出；
- 地图新增矩形框选模式，可列出框选区域内的实体和方块实体；
- 框选结果支持搜索、详情、完整 NBT、复制坐标、地图定位；
- 点按地图对象，或从实体/框选结果定位时，对应图标以黄色覆盖层持续闪烁；
- 点按地图空白位置可取消当前对象选中。

## 0.6.1：低倍率移动续载与高倍率清晰度修复

- “适合屏幕”及展示最多区块的最低倍率下加入四周虚拟平移边距，仍可拖动到新区块并自动续载；
- 地图画布改为按每方块 32 点的整数倍动态尺寸，3×3/5×5/7×7/9×9 均保持方块边界像素对齐；
- 地图位图合成关闭插值和抗锯齿，缩放结束后对缩放比例与偏移做设备像素对齐；
- 实体、方块实体和出生点从地图位图中分离，使用固定屏幕尺寸的矢量覆盖层，放大后保持清晰；
- PNG 导出仍会把对象图层重新合成到图片中。

## 0.6.0：双指缩放、实体与方块实体

### 地图缩放

- 原生双指捏合缩放；
- 根据当前屏幕和动态整数方块画布自动计算最小缩放；
- 最大缩放 32×；
- 双击地图放大；
- 两指轻点地图缩小；
- 快捷选择“适合屏幕、100%、200%、400%”；
- 缩放时显示实时百分比；
- 保存并恢复每个世界的缩放比例；
- 缩放过程中暂停移动续载，结束后再检查视口中心，避免重复渲染。

### 实体与方块实体

实体栏目支持：

- “全部”、主世界、下界和末地；
- 默认扫描选中维度的全部对象，不再按 3×3～9×9 区块窗口限制；
- 支持指定 X/Z 坐标矩形，可选开启 Y0/Y1 范围；也支持指定中心和半径，可选开启 Y 形成 XYZ 球形范围；
- 实体/方块实体分类显示；
- 名称、identifier、UniqueID、坐标和来源搜索；
- 显示坐标、区块、来源及物品槽数量；
- 查看完整 NBT 树，并可进入安全标量编辑；
- NBT 搜索、路径/值复制和原始二进制导出；
- 从列表直接定位到地图；
- 地图显示实体蓝色圆点和方块实体青色方块；
- 地图点按对象查看详情、NBT、坐标与定位；
- 可独立开关实体和方块实体图层。

实体读取同时兼容：

- 现代 `digp<Chunk Key> → actorprefix<ActorUniqueID>` 存储；
- 旧版区块 `Entity (0x32)` 连续 Little-Endian NBT；
- 区块 `BlockEntity (0x31)` 连续 Little-Endian NBT；
- 过渡世界中现代/旧版实体记录按 UniqueID 去重，并优先采用现代 Actor 记录。

实体和方块实体现在支持安全标量写回；现代 Actor 会维护 `digp` 索引，旧实体和方块实体会重建对应区块记录。

## 地图功能

- 地表、高度、矿物和生物群系图层；
- 3×3、5×5、7×7、9×9 作为最小预载区域，缩小时自动扩展到更大的奇数 N×N 区域；
- 拖动跨入新区块后持续续载，缩放结束后按视口覆盖范围扩展区块；
- 单区块缓存和外圈预取；
- 双指缩放、双击放大、两指轻点缩小；
- 主世界、下界、末地切换；
- 区块/方块坐标输入；
- 出生点定位与上次位置恢复；
- 实体、方块实体和可选 HardcodedSpawners 地图图层；
- PNG 导出与解析诊断。

## 其他已实现功能

- `.mcworld`、ZIP、完整世界目录导入和 `.mcworld` 导出；
- 批量世界导入、文件共享目录扫描、从“文件”直接打开；
- 世界列表搜索、排序、重命名、完整复制、删除和导出；
- 世界信息摘要、数据库浏览和手动 `.mcworld` 导出；
- `level.dat` Little-Endian NBT/VarInt NBT 解析与标量编辑；
- 世界 NBT 全树搜索和直接保存；
- 玩家 NBT 列表、搜索、标签编辑和导出；
- 村庄 NBT 兼容旧版 `mVillages` 与新版 `VILLAGE_*` 记录，可查看、编辑、保存和导出；
- 信息菜单中的 Mojang LevelDB 浏览器支持键搜索、复制和原始值导出；
- 已保存结构模板列表、可编辑结构 NBT 与 `.mcstructure` 导出；
- Legacy SubChunk v0/v2-v7 和调色板 SubChunk v1/v8/v9。

## 本地构建

```bash
sudo xcode-select -s /Applications/Xcode_15.4.app/Contents/Developer
brew install xcodegen
cd Blocktopograph-iOS
bash Scripts/bootstrap.sh
open Blocktopograph.xcodeproj
```

生成未签名 IPA：

```bash
bash Scripts/build_unsigned_ipa.sh
```

输出：

```text
build/output/Blocktopograph-iOS13-unsigned.ipa
```

构建脚本会验证成品 `MinimumOSVersion` 必须为 `13.0`。

## 数据写入说明

- 本版本不创建任何自动备份或恢复点；所有写入操作默认用户已提前备份世界。
- “信息 → 导出当前世界”可用于手动导出 `.mcworld`。

## 已知限制

- 方块状态 NBT 已支持调色板 SubChunk v1/v8/v9 写回；旧版数字 ID SubChunk 仍只读。
- 实体和方块实体支持新建、复制、删除、任意 NBT 标签增删改及跨区块坐标迁移。
- 玩家 NBT 支持通用标签增删改，尚未提供背包槽位等专用结构化表单。
- 尚未实现洞穴切片和原版方块纹理；
- `.mcworld` 压缩暂不支持 ZIP64 或超大型世界流式处理；
- 标准 iOS App 无法直接读取 Minecraft 私有沙盒，只能通过导入/导出访问世界。

## 许可证

此衍生工程使用 **AGPL-3.0-or-later**。Mojang 兼容 LevelDB 依赖使用 BSD-3-Clause，详见 `THIRD_PARTY_NOTICES.md`。

## v1.1.0：常加载区块、史莱姆区块与天气编辑

- 地图支持常加载区块和史莱姆区块图层。
- 区块栏目支持管理 LevelDB `tickingarea_` 独立记录，包括新增、编辑、删除与批量预加载操作。
- 信息栏目的世界编辑中支持查看和修改天气。

## v1.1.1：Xcode 15.4 编译修复

- 修复常加载区块圆形标记调用错误的 UIKit 绘图 API。
- 修复导出当前区域时遗漏 `tickingAreas` 参数。
- 将三处 Swift KeyPath 简写改为显式闭包，兼容 Xcode 15.4 / Swift 5。
- 导出常加载区块地图时会重新读取当前维度的 `tickingarea_` 数据。
- 版本更新为 1.1.1（构建号 111）。


## v1.1.2：常加载区域存储格式与选区编辑修复

- 修复旧版把多个区域合并写入单个 `tickingarea` 键、导致 Minecraft 与 `/tickingarea` 无法识别的问题。
- 现在每个区域使用独立的 `tickingarea_<UUID>` LevelDB 键，值为单个 Little-Endian NBT Compound。
- 兼容读取 v1.1.0/v1.1.1 创建的单键连续 NBT；保存时自动迁移为游戏格式。
- 地图框选菜单新增“常加载区域编辑”，可查看、增加、修改和删除与选区相交的区域。
- 单个区块菜单和批量区块菜单新增同样的上下文编辑入口。
- 版本更新为 1.1.2（构建号 112）。


## v1.1.3：实体与方块实体新建、删除及 UniqueID 修改

- 实体栏目右上角新增“+”，可新建现代 `actorprefix` 实体或 `BlockEntity(0x31)` 方块实体。
- 对象详情、长按菜单和侧滑菜单支持复制为新对象与删除整个对象；NBT 编辑器工具栏也提供整对象删除。
- 新建时可使用当前地图选中位置；复制模式会保留原对象完整 NBT，并替换 ID、坐标、维度与实体 UniqueID。
- 删除现代实体会原子清理 `actorprefix` 记录和所在区块 `digp` 引用；旧实体及方块实体会从连续 NBT 记录中精确移除。
- 实体 `UniqueID` 开放修改；保存时自动迁移 actor 键及源/目标 `digp` 引用，并检查 ID 冲突。
- `UniqueID` 仍不能删除或重命名，以避免生成没有主键的现代实体。
- 版本更新为 1.1.3（构建号 113）。



## v1.1.6：现代实体索引与数字方块 ID 支持

- 修复主世界新建/复制现代实体使用错误 `digp + X + Z + DimensionID(0)` 摘要键的问题；主世界现在写入游戏使用的 `digp + X + Z`，下界和末地继续附加维度 ID。
- 打开实体栏目或创建实体时自动迁移 v1.1.3–v1.1.5 写入的错误主世界摘要键，并合并到标准摘要中。
- 删除现代实体时会清理所有 `digp` 引用；实体总览不再把没有任何 `digp` 引用的孤立 `actorprefix` 当作存活实体显示。
- 新建实体基础模板补充 `definitions`、持久化及移动相关基础标签；复制并修改实体 ID 时同步替换对应的实体类型 definition。
- 增加旧版方块数字 ID 0–255 与数据值表字符串 ID 的对应表，并接受现代常用别名反查；旧版 SubChunk 中的数字方块显示为 `minecraft:*` 名称，并保留数字 ID/数据值说明。
- 方块搜索支持使用十进制 ID、十六进制 ID、字符串 ID 或旧式 `legacy:id:data`；替换名称输入数字 ID 时会自动转换为对应字符串 ID。
- “信息 → 基岩版数据值”新增“方块ID”查询入口。
- 版本更新为 1.1.6（构建号 116）。

## v1.1.5：NBT 批量剪贴板与文件导入

- 修复 iOS 剪贴板中批量 NBT 自定义类型被后写入的单标签兼容类型覆盖，导致复制多个标签后实际只能读取第一个标签的问题。
- 批量格式与旧版单标签格式现在一次写入同一个 UIPasteboard 项目；批量负载逐项保存根名称、完整类型和值，并严格验证数量、长度和尾部数据。
- “增加 NBT 标签”菜单新增“导入 NBT／mcstructure／JSON…”，支持导入 Big Endian、Little Endian、Little Endian VarInt、GZip/Zlib、连续多根 NBT、Bedrock mcstructure 和类型化/普通 JSON。
- 向 Compound 导入时使用文件根名称；根名称为空时使用文件名。遇到同名标签继续只询问“覆盖、保留、取消”。
- 向 List 导入时一次加入全部根标签，并校验所有导入项与 List 元素类型一致。
- 独立 NBT 与元数据的“新建根标签”也可从文件导入一个或多个根标签。
- 版本更新为 1.1.5（构建号 115）。

## v1.1.4：常加载半径单位与 NBT 批量粘贴修复

- 圆形常加载区域继续保留游戏存档中的中心方块坐标，但界面半径统一按区块显示和修改；存档中的 64 方块半径会显示为 4 区块。
- 保存圆形区域时把界面输入的区块半径乘以 16 写回 MinX/MaxX/MinZ/MaxZ，并同步修正地图包含判断与选区相交判断。
- v1.1.4 初步加入多标签粘贴流程；部分 iOS 环境仍会因连续写入剪贴板类型而退回单标签，该问题已在 v1.1.5 修复。
- Compound 出现同名标签时只提示“覆盖、保留、取消”；“保留”跳过冲突并继续粘贴其他标签，“覆盖”原位替换同名标签。
- 单个标签与多个标签粘贴均不再弹出修改标签名称的输入框。
- 版本更新为 1.1.4（构建号 114）。
